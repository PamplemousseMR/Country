language: typescript
sudo: false

matrix:
  include:
    #==============================
    #
    #     osx 
    #
    #==============================

    - os: osx
      osx_image: xcode10

    #==============================
    #
    #     linux 
    #
    #==============================
    
    - os: linux

before_install:
  - eval

before_script:
  - npm install -g @angular/cli
  - npm install

script:
  - ng build
  - ng lint